#include <string.h>
#ifndef FUNCTIONS_H
#define FUNCTIONS_H
	int IntLen(const char *cstr);
	bool isNumber(const char *cstr);	
	void changeConlorWithString(std::string colorStr);
	void anlizeString(std::string strRest);
#endif
